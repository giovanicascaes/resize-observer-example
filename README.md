A small React project showing an implementation of responsive components using ResizeObserver API and CSS child and descendant selectors

### How to run

Navigate to directory and run `yarn && yarn start`